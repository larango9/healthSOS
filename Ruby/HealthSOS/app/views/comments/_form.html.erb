<%= form_for([@post, @post.comments.build]) do |f| %>
  <p>
    <%= f.label :Nombre %><br />
    <%= f.text_field :commenter %>
  </p>
  <p>
    <%= f.label :Comentario %><br />
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit :Guardar %>
  </p>
<% end %>
